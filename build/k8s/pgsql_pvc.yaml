kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgresql
  namespace: go-api-pgsql-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: thin
